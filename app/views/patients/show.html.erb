<h1><%= @patient.name %>
<% @patient.appointments.each do |appt| %>

    <p><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></p>

<% end %>