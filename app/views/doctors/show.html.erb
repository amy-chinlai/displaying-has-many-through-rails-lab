<%= @doc.name %>
<% @doc.appointments.each do |appt| %>

   <p> <%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %H:%M"), appointment_path(appt) %> - <%= link_to appt.patient.name, patient_path(appt.patient) %></p><br>

<% end %>